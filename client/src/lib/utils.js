// Tiny helper for conditional Tailwind classes, with tailwind-merge resolving conflicts.
import { clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs) {
  return twMerge(clsx(inputs));
}